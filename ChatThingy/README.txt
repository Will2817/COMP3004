README - Chat Program

The server broadcasts any messages that are sent to all clients, including the sender

Reference the lidgren dll to build

Type /name <name> to set name to <name>
From the main menu, type /host to host a chat
From the main menu, type /join <ip> to join a chat being hosted at <ip>
Disconnecting from host to be implemented